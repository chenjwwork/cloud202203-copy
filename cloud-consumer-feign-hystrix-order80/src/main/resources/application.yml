server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:7001/eureka/
#开启fegin中hystrix属性
feign:
  hystrix:
    enabled: true